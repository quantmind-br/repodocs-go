// Package converter provides the HTML to Markdown conversion pipeline.
//
// The pipeline normalizes document encoding, extracts readable content,
// sanitizes unsafe or noisy markup, and renders the result as Markdown. It also
// includes auxiliary readers for Markdown, plain text, and other input formats.
package converter
